import { ChangeDetectionStrategy } from "angular2/core";

// So we can conveniently flip between "OnPush" and the default ("CheckAlways")  
export const STRATEGY: ChangeDetectionStrategy = ChangeDetectionStrategy.OnPush;


